/**
 * frontend/src/constants/countries.ts
 *
 * Thin shim — re-exports from @meridian/shared so the component does
 * not need to change its import path.
 */
export { SUPPORTED_COUNTRIES } from "@meridian/shared";
export type { CountryOption } from "@meridian/shared";
