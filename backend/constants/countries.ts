/**
 * backend/constants/countries.ts
 *
 * Re-exports from @meridian/shared.
 * The canonical country list lives in the shared package so the
 * frontend and backend stay in sync automatically.
 */
export {
  SUPPORTED_COUNTRIES,
  SUPPORTED_COUNTRY_CODES,
} from "@meridian/shared";
export type { CountryOption } from "@meridian/shared";
